import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic-order-webapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
